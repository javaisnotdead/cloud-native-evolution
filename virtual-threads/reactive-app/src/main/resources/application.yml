spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/orders
    username: orders
    password: orders
    pool:
      max-size: 20
  sql:
    init:
      mode: always

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
